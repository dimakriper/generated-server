{
  "name": "generated-server",
  "version": "1.0.0",
  "description": "Spring Boot Server",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "powershell -Command \"if (Test-Path src/main/resources/static) { Remove-Item -Path src/main/resources/static/* -Recurse -Force }\"",
    "build:prod": "npm run clean && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vue": "^3.5.16",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "rimraf": "^6.0.1",
    "vite": "^6.3.5"
  }
}
